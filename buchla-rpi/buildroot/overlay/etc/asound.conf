# Buchla 700 — ALSA config
# card 0 = HDMI, card 1 = Headphone jack
# type plug does automatic format conversion (float32 -> int16/int32)
pcm.!default {
    type plug
    slave.pcm "hw:1,0"
}

ctl.!default {
    type hw
    card 1
}
